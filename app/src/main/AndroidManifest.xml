<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="logic.global.manualpressure" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ManaulPressureActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="triangle.scroll.TriangleActivity"
            android:label="@string/title_activity_triangle" >
        </activity>
        <activity
            android:name=".CircularSeekBarActivity"
            android:label="@string/title_activity_circular_seek_bar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".ValveFlowRate"
            android:label="@string/title_activity_valve_flow_rate" >

        </activity>
    </application>

</manifest>
